# Nginx 配置示例 - API 网关反向代理
# 将域名请求转发到后端网关（localhost:9000）

server {
    listen 80;
    server_name api.localhost;  # 或者使用你的实际域名，如 api.yourdomain.com
    
    # 可选：支持 HTTPS
    # listen 443 ssl;
    # ssl_certificate /path/to/cert.pem;
    # ssl_certificate_key /path/to/key.pem;
    
    # 允许跨域（如果需要）
    add_header Access-Control-Allow-Origin *;
    add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, PATCH, OPTIONS';
    add_header Access-Control-Allow-Headers 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization';
    
    # 处理预检请求
    if ($request_method = 'OPTIONS') {
        return 204;
    }
    
    # 代理所有 API 请求到网关
    location / {
        proxy_pass http://localhost:9000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # 如果需要按路径前缀路由到不同服务（网关不支持时）
    # location /auth/api/v1 {
    #     proxy_pass http://localhost:8096/api/v1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }
    # 
    # location /config/api/v1 {
    #     proxy_pass http://localhost:8097/api/v1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }
    # 
    # location /job/api/v1 {
    #     proxy_pass http://115.190.196.117:8098/api/v1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }
    # 
    # location /mq/api/v1 {
    #     proxy_pass http://115.190.196.117:8099/api/v1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }
}

