# 2026-06-23

## 生成 CODE_STYLE.md 项目编码规范

- 任务：从项目配置和典型源码提炼 Vue3 + TS 编码规范，输出适合 RAG 知识库的 `CODE_STYLE.md`
- 输出文件：`CODE_STYLE.md`（607 行 → 630+ 行，位于项目根目录）
- 覆盖内容：
  - 工程基础化配置（ESLint `.eslintrc.js`、tsconfig、路径别名）
  - Vue3 组件范式（统一 `<script setup lang="ts">`，代码组织顺序，Props/Emits/Style 规范）
  - TypeScript 规范（Interface/Type/Enum 命名与存放，`as`/`!` 使用边界，`any` 规范）
  - 典型对比代码范本（组件写法、Pinia Store、API 层、Composable、类型定义）
  - 目录结构与文件命名约定
- 关键发现：项目 TS 配置为宽松模式（strict: false），ESLint any 为 warn 级，无 Prettier，使用 SCSS + Less 双预处理器

## Dify RAG 检索优化

- 问题：Dify 召回测试中，自然语言 Query（如"接口调用示例"）返回空结果 `records: []`；精确匹配章节标题则能命中（Score 0.99）
- 根因：Query 语义与文档实际词汇不匹配（"接口" vs "API"、"调用示例" vs "写法"），加上 Score 阈值过滤了低分候选
- 解决方案：
  1. 在每个 `##` 一级章节下插入 HTML 注释形式的 RAG 锚点（同义词 + 自然语言表达 + 核心关键词）
  2. 建议用户将 Score 阈值从 0.5 降至 0.3
  3. 建议重新上传文档触发重新向量化
- 配置建议：父子分段（`##`/`###`）、高质量索引、混合检索(语义0.6/关键词0.4)、BGE-large-zh-v1.5 Embedding
